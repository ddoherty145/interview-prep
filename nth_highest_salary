CREATE FUNCTION getNthHighestSalary(N INT) RETURNS INT
DETERMINISTIC
READS SQL DATA
BEGIN
   DECLARE result INT;
   SET N = N - 1;

   SELECT DISTINCT Salary
   INTO result
   FROM Employee
   ORDER BY Salary DESC
   LIMIT 1 OFFSET N;

   RETURN result;
END;
